import React from "react";
import { MenuProps } from ".";

const Web: React.FunctionComponent<MenuProps> = ({ menuItems }) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default Web;
